# Frontend handoff area

Samson can build the React/Vite interface against `../docs/api-contract.md` and the running backend. The API returns deterministic simulated fixtures until the database and intelligence handoffs are connected.

