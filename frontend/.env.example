# Use the MEDRIPPLE fixture backend supplied in ../backend for local integration.
VITE_API_BASE_URL=http://127.0.0.1:3001/api
# Set true only when demonstrating the bundled fallback data without the backend.
VITE_USE_MOCKS=false
