# Database handoff area

Dhiren should add MySQL migrations, a deterministic seed/reset command, and a data dictionary here. When the schema is frozen, replace the backend fixture-store implementation without changing the published route contract unless the team agrees a version change.

